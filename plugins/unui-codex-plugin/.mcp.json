{
  "mcpServers": {
    "unui-mcp": {
      "type": "http",
      "url": "http://api.unui.cc/v1/mcp",
      "oauth_resource": "http://api.unui.cc/v1/mcp",
      "scopes": ["style:read"],
      "enabled_tools": ["unui_auth_status", "unui_evidence_pack"],
      "note": "UNUI MCP server. Uses OAuth on first connection."
    }
  }
}
